# Changelog

## 0.4.0

- انتخاب صریح `draft`، `pending` یا `published` در همان درخواست
- پشتیبانی از `publish_now` به‌عنوان انتخاب بولی جایگزین
- خطای روشن در صورت حذف یا تناقض انتخاب انتشار
- جلوگیری از پاسخ موفق کاذب برای درخواست فاقد فیلد محتوایی
- سازگاری با قرارداد تیم شامل `h1`، `seo_description`، `url` و `category_url`
- پشتیبانی یکسان از قالب تو‌در‌توی Yoast برای دسته و محصول
- ذخیره صحیح متای taxonomy در Yoast و بازسازی indexable دسته
- بازسازی indexable محصول پس از تغییر مستقیم متای Yoast
- اعتبارسنجی slug، canonical و دسته والد
- پاسخ‌های workflow، verification و warnings و هدر `X-Chrono-Status`
- جستجوی دقیق دسته با `slug` و `external_id`
- هدرهای کامل صفحه‌بندی فهرست دسته‌ها
- احراز هویت اختیاری با `CHRONOIRAN_SEO_API_KEY`
- بهبود endpoint سلامت و پیام‌های خطای فارسی
- مستندات فارسی کامل برای REST API، cURL و n8n

## 0.3.0

- اصلاح ذخیره متای Yoast برای دسته‌های ووکامرس
- پشتیبانی از قالب تو‌در‌توی `yoast` و نام‌های جایگزین `title` و `content`
- پذیرش canonical خالی

## 0.2.0

- گردش کار پیش‌نویس و انتشار برای دسته‌ها و محصولات
- upsert دسته با external ID یا slug
- endpoint پیش‌نمایش و بررسی محتوا
